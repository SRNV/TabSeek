/**
 * @file NoteComponents.tsx
 * Re-export barrel for the note pod scene components.
 * Each component lives in its own file; this file exists only for backward-compatible
 * imports in code that was written before the one-component-per-file refactor.
 */
export { NoteDisc } from './NoteDisc'
export { SubNoteBody } from './SubNoteBody'
export { passWheel } from './passWheel'
