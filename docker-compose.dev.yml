services:
  app:
    image: node:22-alpine
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules   # isolates container node_modules from host
    ports:
      - "5173:5173"
    command: sh -c "npm install && npm run dev -- --host"
